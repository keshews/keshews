int x = 5
if x > 10 {
    x += 1
    // the lack of symbol table means this line shouldn't give error 
    y += 1
}

// should give error
elif {
    while(x>2) {
        x += 1
      }
  }

for ( int i = 0; i<n; i += 1) {
    h = 2
  }

